{
  "name": "@renegaderocks/utility",
  "module": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "bun run clean && bun build ./src/index.ts --outdir ./dist --minify",
    "clean": "rm -rf ./dist",
    "publish:major": "bun publish.js version=major",
    "publish:minor": "bun publish.js version=minor",
    "publish:patch": "bun publish.js version=patch"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
